machine:
  services:
    - docker

test:
  override:
    - make build REPO=$USER

deployment:
  any:
    branch: /.*/
    commands:
      - docker login -e="." -u="$DOCKER_USER" -p="$DOCKER_AUTH" quay.io
      - make deploy REPO=$USER